{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Verilog Testbench",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/${fileBasenameNoExtension}.out",
            "preLaunchTask": "Verilog Build",
            "cwd": "${workspaceFolder}",
            "externalConsole": true,
            "MIMode": "gdb"
        }
    ]
}